<li><hr role="separator" class="dropdown-divider"></li>

<li> <a class="dropdown-item" href="#">
        Reserves &raquo;
      </a>
      <ul class="dropdown-menu dropdown-submenu">
        <li><a class="dropdown-item" href="#" {{action 'desperatePool'}} class="dropdown-item">Desperate Reserve Spend</a></li>
        <li><a class="dropdown-item" href="#" {{action (mut this.selectSpendPool) true}} class="dropdown-item">Spend Reserve</a></li>
        <li><a class="dropdown-item" href="#" {{action (mut this.selectAddPool) true}} class="dropdown-item">Add Reserve</a></li>
        <li><a class="dropdown-item" href="#" {{action 'resetPool'}} class="dropdown-item">Reset Reserve</a></li>
        <li><a class="dropdown-item" href="#" {{action 'showPool'}} class="dropdown-item">Show Reserve</a></li>
      </ul>
</li>

<BsModalSimple @title="Spend Reserve" @closeTitle="Cancel" @submitTitle="Spend" @size={{null}} @fade={{true}} @open={{this.selectSpendPool}} @onSubmit={{action "spendPool"}} @onHide={{action (mut this.selectSpendPool) false}}>

  <b>Amount:</b> <Input @type="number" step="1" size={{25}} id="poolSpendAmount" @enter={{action "spendPool"}} @value={{this.poolSpendAmount}} />
    
  <b>Reason:</b> <Input @type="text" size={{25}} id="poolSpendReason" @enter={{action "spendPool"}} @value={{this.poolSpendReason}} />

</BsModalSimple>

<BsModalSimple @title="Add Reserve" @closeTitle="Cancel" @submitTitle="Add" @size={{null}} @fade={{true}} @open={{this.selectAddPool}} @onSubmit={{action "addPool"}} @onHide={{action (mut this.selectAddPool) false}}>

  <b>Amount:</b> <Input @type="number" step="1" size={{25}} id="poolAddAmount" @enter={{action "addPool"}} @value={{this.poolAddAmount}} />
    
  <b>Reason:</b> <Input @type="text" size={{25}} id="poolAddReason" @enter={{action "addPool"}} @value={{this.poolAddReason}} />

</BsModalSimple>
